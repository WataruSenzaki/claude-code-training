# 設計の根拠（なぜこの形か）

> この教材を「なぜこの Layer 構成にしたか・どこまで調べて何を入れ／外したか」を残す設計ドキュメント。
> Layer 構成そのもの・各 Layer の中身・ディレクトリ構成は [README.md](./README.md)。ここは README の「理由づけ」担当。用語の定義も README の `## 用語` が正本。
> 作り手との進め方（AI引継ぎ）は [CLAUDE.md](./CLAUDE.md)。

## 設計の狙い（局所化したいもの）

ターゲットが変わったときの**「変更ダメージ」を局所化**する。普遍の部分（L1・L2、各 Layer の Template）と、対象ごとに差し替える部分（L3）を構造で分離する。これが Layer に分けた目的。研修の入口（導入）も対象で流れは変わらないので Layer に切り出さず L1 Bedrock に統合し、対象差（完成品の例・刺し方）は Add-on に局所化する。

## なぜ capability を3つの Layer にしたか — 「必須」が2種類あるから

- 必須には2種類ある: **機能的必須**（ないと動かない）と **価値的必須**（CCの強味＝振る舞いを指定できる、を伝えられるか）
- 判定テストが機能1本だと後者を測れず、「CLAUDE.md は必須にしたいのに機能的には必須でない」という矛盾が出た
- これを Layer に割り当てて解決した: **L1＝機能的必須（動く）／ L2＝価値の源（橋渡し）／ L3＝タイプごとに何を必須にするか決める**。2種類の必須が Layer として分離する
- だから各 Layer は「ゴール」で定義している（L1=動く／L3=自社業務につながる／L2=その橋渡し）

## なぜ研修の入口を Layer に切り出さず Bedrock に統合したか（Compass を畳んだ経緯・2026-06-01）

一度「研修の入口」を **L0 Compass** という別 Layer に切り出したが、同日中に畳んで L1 Bedrock の最初の Unit「導入」に統合し直した。経緯と判断を記録する（再分割を防ぐため）。

- **切り出した動機**: 「導入」が2つを抱えて見えた — 研修の導入（なぜここに・どこへ）と CC の導入（何の道具か）。入口は対象で変わるから可変 Layer に隔離したい、と考えた
- **畳んだ理由1（動線の逆流）**: Compass で「だから始めよう」と動機の山場を作った後に、Bedrock で「ところで CC とは何か」を説明する流れは因果が逆。「なぜ流行る？＝チャットとの違い」は **CC の正体の説明そのもの**で、動機を作る材料。動機の山場の後ろには置けず、入口の一連の話として前に来る。切り離すと動線が壊れる
- **畳んだ理由2（不変性）**: Layer に切り出す唯一の根拠は「入口は目的で変わる＝可変」だった。しかし入口の流れ（流行る理由＝CCとは → 完成品イメージ → 使うには土台が要る → 契約へ）は**対象が変わっても構造が変わらない**。変わるのは完成品の例・刺し方だけ＝**Add-on レベル**。可変性が Add-on 止まりなら Layer を立てるのは過剰。この教材自身の Layer の定義（ゴール／テストで定義し、分けて変更ダメージを局所化する）にも Compass は当てはまらない（テストが曖昧・出口ゲート無し・構造変更を局所化しない）
- **残した収穫（Layer は畳んだが中身は活きる）**: 壁打ちで得た原則は Bedrock 導入にそのまま使う —
  - **動機は前提であって出力ではない**。受講者は「自分で楽になりたい」を持って来る。焚きつけず、既存の関心に「CC とは何か→完成品（行き先）→土台は不可避」を接続する
  - **報酬は L2/L3 側**（貯まる・自社業務）にあり、**L1 はそこへの避けて通れない土台**。完成品（行き先）を見せ「土台は飛ばせない」と接続する
  - **チャットとの違いは「理解」として使う**（売り込みでない）。違いは1点＝ローカルファイルの読み書きに集約し、覚える・代行はそこから来る利点として置く。動機を持つ受講者に「すごいだろ」は宙に浮く

## 抜け調査の要点（2026-05-28・公式docs再検証）

「非エンジニアが install→使えるまで」に抜けがないか公式ドキュメントで再検証した結果:

- **足した抜け**: 「使い続ける」（セッション再開 `--continue`/`--resume`、`/clear`・コンテキスト詰まり、`Esc`中断、終了）→ L1基本操作へ。最初の貼り付けキー(Win/Mac)→セットアップのAdd-on。「AIは自信満々に間違える→鵜呑みにせず確認」→責任ある利用。CLAUDE.md不整合（導入で"メモリ"を謳うのに教える場がない）→ L2の目玉に
- **意図的に外した**（非エンジニアには過剰）: 権限モード6段階（auto/dontAsk/bypassPermissions/CI）、エンタープライズのコスト管理・MCP深掘り・Hooks・protected paths。**デフォルト＝全部確認、だけで十分**

### 出典

code.claude.com/docs の quickstart / common-workflows / sessions / best-practices / terminal-guide / permission-modes / memory / costs / troubleshooting / security
